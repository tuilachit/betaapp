import SwiftUI

struct ReasiTheme {
    let colors = Color.reasi
    let spacing = ReasiSpacing.self
    let radius = ReasiRadius.self
    let motion = ReasiMotion.self
    let typography = ReasiTypography.self

    static let current = ReasiTheme()
}

